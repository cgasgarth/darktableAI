#!/usr/bin/env sh
set -eu

while read -r local_ref local_sha remote_ref remote_sha
 do
  if [ "$local_ref" = "refs/heads/main" ] || [ "$remote_ref" = "refs/heads/main" ]; then
    echo "Direct pushes to main are blocked. Create a branch and open a PR instead."
    exit 1
  fi
 done
